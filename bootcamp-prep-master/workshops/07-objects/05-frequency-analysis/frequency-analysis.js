// YOUR CODE BELOW
function frequencyAnalysis(string) {
    let key = []
    let val = []
    for (let i = 0; i < string.length; i++) {
        if (keys.includes(string[i] === false)) {
            keys.push(string[i])
        }
    }
}