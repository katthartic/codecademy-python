// YOUR CODE BELOW
function greeting(name) {
    if (name) {
        return `Hello ${name}!`;
    } else {
        return 'Hello!';
    }
}

/*
function greeting(name) {
    greeting = ''
    name ? greeting = `Hello ${name}!` : greeting = 'Hello!';
    return greeting
}
*/